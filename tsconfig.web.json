{
  "compilerOptions": {
    "composite": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    // dead code is a build failure, not a review note (audit #8) — ESLint covers
    // the stylistic half, this covers the accidental half, for free in CI
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "isolatedModules": true,
    "useDefineForClassFields": true
  },
  "include": ["src/web/**/*.tsx", "src/web/**/*.ts", "src/shared/**/*.ts"]
}
